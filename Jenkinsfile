pipeline {
    agent any

    stages {
        stage('Zero') {
            steps {
                echo 'Zero'
            }
        }
        stage('One') {
            steps {
               echo 'One'
            }
         }
    }
}