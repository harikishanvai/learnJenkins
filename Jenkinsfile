pipeline {
    agent any

    stages {
        stage('Zero') {
            steps {
                ansiColor('xterm') {
                    // some block
                }
                echo 'Zero'
                sh '''echo vai
                echo love'''
            }
        }
        stage('One') {
            steps {
               echo 'One'
            }
         }
    }
}